@model IEnumerable<POSforRestaurants.WebUI.Models.SeatView>

@{
    ViewBag.Title = "Seats";
}

<h2>Seats</h2>

<p>
    @Html.ActionLink("Create New", "AddSeat")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SeatId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SeatId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.SeatId  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.SeatId  })
        </td>
    </tr>
}

</table>
